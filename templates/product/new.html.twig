{% extends 'base.html.twig' %}

{% block title %}Add Product{% endblock %}

{% block body %}
    <h2>Add Product</h2>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button>Save</button>
    {{ form_end(form) }}
{% endblock %}
